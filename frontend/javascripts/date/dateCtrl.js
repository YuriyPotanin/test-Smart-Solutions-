var app = require('../app');
var resourceFactory = require('../resourceFactory');

angular.module('controlTimeModule')
	.controller('dateCtrl', dateCtrl);

dateCtrl.$inject = ['resourceFactory'];

function dateCtrl (resourceFactory) {

	
}